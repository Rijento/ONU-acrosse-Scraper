import javax.swing.*;
import java.awt.*;

public class PlayerPanel extends JPanel {
    public PlayerPanel(Player player, String year) {
        super();
        this.setLayout(new FlowLayout()); // we are using a flow layout
    }
}
